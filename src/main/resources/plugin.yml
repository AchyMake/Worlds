name: Worlds
version: '${project.version}'
main: net.achymake.worlds.Worlds
api-version: 1.13
author: AchyMake
load: POSTWORLD
prefix: '${project.name}'
description: '${project.description}'
commands:
  worlds:
    description: add or create worlds
    permission: worlds.command
    usage: /worlds
permissions:
  worlds.command:
    default: op
    description: allows to use worlds commands
